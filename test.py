#!/usr/bin/python3
import dbinterafce as db
import time
# db.createDataBase("qwerty","token")

# result = db.getPublicKeySU()
# print(result)

# result = db.getPublicKey(1)
# print(result)

# result = db.getBalance(100,"token")
# print(result)

# txn_id, sender_id, receiver_id, send_bal, recev_bal, time_stamp, token_title
# result = db.updateAmountDB(1, 100, 1, 500, 1000, time.time(),"token")
# print(result)

# result = db.getTotalSupply("token")
# print(result)

# token_title, amount
# result = db.tokenUpdate("token",10000)
# print(result)

# req_id, session_id, sender_id, receiver_id, amount, time_stamp, token_title
# result = db.updateDBRequest(1,1000,1,2,500,time.time(),"token")
# print(result)
